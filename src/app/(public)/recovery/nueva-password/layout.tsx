import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Nueva Contraseña — Logia Caleuche 250',
};

export default function NuevaPasswordLayout({ children }: { children: React.ReactNode }) {
    return children;
}
